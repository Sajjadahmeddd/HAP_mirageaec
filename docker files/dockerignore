# Frontend build context. node_modules is the big one — npm ci installs a
# clean tree inside the image, so copying the host's is pure waste and can
# break the build when host and container architectures differ.

node_modules
dist
.git
.env
.env.*
!.env.example
npm-debug.log*
.DS_Store
.vscode/
.idea/
coverage/
Dockerfile
.dockerignore
