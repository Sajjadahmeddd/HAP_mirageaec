"""AirSizer Pro — catalog-based diffuser selection and sizing (module 2).

Reads the schedule HAPExt produced, sizes a diffuser per subspace against the
TECNALCO catalogs, and exports a sized Excel. `engine/` never imports from
`ui/`, the same rule the HAPExt module follows.
"""
